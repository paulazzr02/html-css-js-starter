# HTML/CSS/JS Starter

효율적인 UIUX 개발을 위한 범용 퍼블리싱 템플릿 시스템

> **템플릿 사용 안내**: 이 템플릿은 개별 웹 사이트 퍼블리싱을 위한 기본 틀로 사용됩니다.
> 각 프로젝트에 맞게 커스터마이징하여 사용하세요.

## 📁 프로젝트 구조

```
루트/
├── config/
│   ├── site.config.js        # 사이트 및 빌드 설정 (프로젝트별 수정 필요)
│   └── scripts/              # 시스템 스크립트
│       ├── generate-page.js  # 페이지 자동 생성
│       ├── convert-figma-tokens.js # Figma 토큰 변환
│       └── test-accessibility.js  # 접근성 테스트
├── public/                   # 정적 자원 (변환 없이 그대로 복사)
│   ├── fonts/                # 폰트 (Pretendard, Material Icons 포함)
│   ├── img/                  # 이미지
│   └── favicon.svg           # 파비콘
├── src/
│   ├── scripts/              # JavaScript 소스 (빌드 대상)
│   ├── styles/               # SCSS 소스 (컴파일 대상)
│   │   ├── styles.scss       # 메인 진입점
│   │   ├── abstracts/         # 변수, 믹스인, 함수 등
│   │   ├── base/              # 기본 스타일 (reboot, typography 등)
│   │   ├── layout/            # 레이아웃 스타일
│   │   ├── components/        # 컴포넌트 스타일
│   │   ├── pages/             # 페이지별 스타일
│   │   ├── utilities/         # 유틸리티 클래스
│   │   └── vendors/           # 외부 라이브러리 (Pretendard, Material Icons 등)
│   ├── pages/                # HTML 페이지
│   ├── templates/            # HTML 인클루드 템플릿
│   └── index.html            # 메인 페이지
├── dist/                     # 빌드 결과물 (자동 생성)
├── gulpfile.js               # Gulp 빌드 설정
└── package.json              # 프로젝트 의존성
```

## 🚀 시작하기

### 1. 설치

```bash
npm install
```

### 2. 설정

프로젝트 설정을 위해 `config/site.config.example.js`를 참고하여 `config/site.config.js`를 생성하고 수정하세요:

```bash
# Linux/Mac
cp config/site.config.example.js config/site.config.js

# Windows
copy config\site.config.example.js config\site.config.js
```

주요 설정 항목:

- **경로 설정**: 소스 및 빌드 결과물 경로
- **개발 서버 설정**: 포트, 브라우저 자동 열기 등
- **언어 설정**: HTML lang 속성
- **Viewport 설정**: 반응형/적응형 모드 선택

자세한 설정 옵션은 `config/site.config.example.js` 파일의 주석을 참고하세요.

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

### 4. 빌드

```bash
npm run build
```

빌드 결과물은 `dist/` 폴더에 생성됩니다.

### 빌드 타입

- **프로덕션 빌드** (`npm run build`): 상대경로 사용, file:// 프로토콜 지원, 최적화된 빌드
  - 브라우저에서 직접 열 수 있음 (ERR_FILE_NOT_FOUND 오류 없음)
  - 웹 서버 없이도 정상 작동

## 🧭 주요 명령어

```bash
# 개발 서버 실행 (자동 리로드)
npm run dev
# 또는
npm start

# 프로덕션 빌드 (상대경로, file:// 프로토콜 지원)
npm run build

# 빌드 결과물 삭제
npm run clean

# 새 페이지 생성
npm run new <페이지명>

# 코드 포맷팅
npm run format

# 웹 접근성 테스트
npm run test:a11y
```

## 🔧 개발 환경

- **Node.js**: >=18.17.1 (`.nvmrc` 파일 참고)
- **Gulp**: 4.x
- **Sass**: Dart Sass
- **BrowserSync**: 자동 리로드

### Node.js 버전 관리

프로젝트는 `.nvmrc` 파일로 Node.js 버전을 관리합니다. nvm을 사용하는 경우:

```bash
# Node.js 버전 자동 전환
nvm use

# 또는 특정 버전 설치
nvm install 18.17.1
```

### HTML 인클루드

`gulp-file-include`를 사용하여 HTML 파일을 모듈화:

```html
@@include('../templates/_header.html')
```

접두사는 `config/site.config.js`의 `build.html.prefix`에서 변경 가능 (기본값: `'@@'`).

**참고**: 템플릿 경로는 `src/templates/` 디렉토리를 기준으로 합니다.

## 🧩 문제 해결

### 빌드 오류 시

```bash
# 의존성 재설치
rm -rf node_modules && npm install

# 빌드 결과물 삭제 후 재빌드
npm run clean && npm run build
```

## ♿ 웹 접근성 테스트

```bash
# 기본 URL 테스트
npm run test:a11y

# 특정 URL 테스트
npm run test:a11y -- --url http://localhost:3000/html/about.html
```
