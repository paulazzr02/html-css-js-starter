# HTML/CSS/JS Starter

효율적인 UIUX 개발을 위한 범용 퍼블리싱 템플릿 시스템

> **템플릿 사용 안내**: 이 템플릿은 개별 웹 사이트 퍼블리싱을 위한 기본 틀로 사용됩니다.
> 각 프로젝트에 맞게 커스터마이징하여 사용하세요.

## 📁 프로젝트 구조

```
루트/
├── config/
│   └── site.config.js        # 사이트 및 빌드 설정 (프로젝트별 수정 필요)
├── scripts/                   # 시스템 스크립트
│   ├── generate-page.js      # 페이지 자동 생성
│   └── test-accessibility.js  # 접근성 테스트
├── src/
│   ├── assets/
│   │   ├── scss/             # SCSS 소스
│   │   │   ├── main.scss     # 메인 진입점
│   │   │   ├── common/       # 공통 스타일
│   │   │   │   ├── _variables.scss # 프로젝트 변수 (Semantic Token)
│   │   │   │   ├── _root.scss      # CSS 변수 정의
│   │   │   │   ├── _krds-tokens.scss # KRDS Primitive Token
│   │   │   │   ├── _pretendard.scss # Pretendard 폰트
│   │   │   │   └── features/       # Feature 컴포넌트
│   │   │   ├── components/   # 프로젝트 컴포넌트 (선택)
│   │   │   └── pages/        # 페이지별 스타일 (선택)
│   │   ├── js/               # JavaScript
│   │   ├── img/              # 이미지
│   │   └── fonts/             # 폰트 (Pretendard 포함)
│   ├── html/                 # HTML 페이지
│   ├── includes/             # HTML 인클루드 템플릿
│   └── index.html            # 메인 페이지
├── dist/                     # 빌드 결과물 (자동 생성)
├── gulpfile.js               # Gulp 빌드 설정
└── package.json              # 프로젝트 의존성
```

## 🚀 시작하기

### 1. 설치

```bash
npm install
```

### 2. 설정

`config/site.config.js`에서 프로젝트 설정 수정:

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

### 4. 빌드

```bash
npm run build
```

빌드 결과물은 `dist/` 폴더에 생성됩니다.

### 빌드 타입

- **프로덕션 빌드** (`npm run build`): 절대경로 사용, 최적화된 빌드
- **검수용 빌드** (`npm run review`): 상대경로 사용, file:// 프로토콜 지원, 메일 전송 등에 유용

## 📝 주요 명령어

```bash
# 개발 서버 실행 (자동 리로드)
npm run dev
# 또는
npm start

# 프로덕션 빌드
npm run build

# 검수용 빌드
npm run review

# 빌드 결과물 삭제
npm run clean

# 새 페이지 생성
npm run new <페이지명>

# 코드 포맷팅
npm run format

# 웹 접근성 테스트
npm run test:a11y
```

## 🔧 개발 환경

- **Node.js**: >=18.17.1
- **Gulp**: 4.x
- **Sass**: Dart Sass
- **BrowserSync**: 자동 리로드

### HTML 인클루드

`gulp-file-include`를 사용하여 HTML 파일을 모듈화:

```html
@@include('../includes/_header.html')
```

접두사는 `config/site.config.js`의 `build.html.prefix`에서 변경 가능 (기본값: `'@@'`).

## 🆘 문제 해결

### 빌드 오류 시

```bash
# 의존성 재설치
rm -rf node_modules && npm install

# 빌드 결과물 삭제 후 재빌드
npm run clean && npm run build
```

## ♿ 웹 접근성 테스트

```bash
# 기본 URL 테스트
npm run test:a11y

# 특정 URL 테스트
npm run test:a11y -- --url http://localhost:3000/html/about.html
```
